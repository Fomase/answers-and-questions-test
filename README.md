# answers-and-questions-test
## Установка:
- Заполнить .env файл по примеру .env.example
- Выполнить команды:
- `docker-compose up --build`
- `docker-compose run --rm backend sh -c "python manage.py migrate"`
